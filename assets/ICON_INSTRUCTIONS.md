# SignBridge application icon

Place a Windows icon named `signbridge.ico` in this folder, then run
`build_signbridge.bat` again. The build automatically uses it for `SignBridge.exe`.

Until an icon is supplied, Windows uses the default executable icon.
